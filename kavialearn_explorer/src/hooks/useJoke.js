/**
 * PUBLIC_INTERFACE
 * Hook for random joke (stub).
 */
export default function useJoke() {
  // TODO: Fetch real joke
  const joke = "Why was the math book sad? It had too many problems!";
  return { joke };
}
